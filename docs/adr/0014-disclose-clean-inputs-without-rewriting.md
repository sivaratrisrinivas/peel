# Disclose clean inputs without rewriting

When every Attack returns zero Findings, Peel skips Repair Approval and preserves the workbook byte-for-byte. Disclosure remains blocked unless Claimed Scope is `aligned`, and `send_email` still requires Disclosure Approval. `mismatch` and `insufficient_context` block Disclosure even when the container is technically clean.
